# SignBridge Backend Dependencies
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic[email]>=2.9.0
pydantic-settings>=2.6.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0

# WebSocket & HTTP
websockets>=13.0
httpx>=0.27.0

# AI / ML (optional — install for full functionality)
# Uncomment for GPU: torch with CUDA
numpy>=1.26.0
opencv-python-headless>=4.10.0
mediapipe>=0.10.14

# Speech Recognition (optional — large download)
faster-whisper>=1.0.3

# Translation (optional — large download)
transformers>=4.46.0
sentencepiece>=0.2.0
sacremoses>=0.1.1

# Logging & utilities
python-dotenv>=1.0.1

# Production
gunicorn>=23.0.0
